resources:
  pipelines:
    scctower_sdp:
      name: scctower_sdp
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      configuration:
        spark.databricks.delta.enableTimestampNtz: "true"
      libraries:
        - glob:
            include: ../jobs/scctower_sdp/**
